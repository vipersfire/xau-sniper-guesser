-- Placeholder: SQLAlchemy creates all tables via init_db.py
-- This file exists so docker-entrypoint-initdb.d picks it up cleanly.
-- Run: python scripts/init_db.py   after the container is healthy.
SELECT 1;
